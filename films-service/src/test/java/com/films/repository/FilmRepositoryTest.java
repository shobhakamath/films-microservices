package com.films.repository;

//@DataMongoTest
public class FilmRepositoryTest {

   /* @Autowired
    FilmRepository filmRepository;

    @Before
    public void setUp() throws Exception {
        filmRepository.save(new Film("film", "string1", "string2",
            LocalDateTime.now(), Rating.ONE_STAR,
            new BigDecimal(2000), "", Genre.ANIMATION, null));
    }

    @Test
    public void shouldBeNotEmpty() {
        assertThat(filmRepository.findAll()).isEmpty();
    }*/
}
